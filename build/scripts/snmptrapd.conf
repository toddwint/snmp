
#
# EXAMPLE-trap.conf:
#   An example configuration file for configuring the Net-SNMP snmptrapd agent.
#
###############################################################################
#
# This file is intended to only be an example.
# When the snmptrapd agent starts up, this is where it will look for it.
#
# All lines beginning with a '#' are comments and are intended for you
# to read.  All other lines are configuration commands for the agent.

#
# PLEASE: read the snmptrapd.conf(5) manual page as well!
#
#authCommunity log,execute,net private　
authCommunity log,execute,net public
#
## send mail when get any events
#traphandle default /usr/bin/traptoemail -s smtp.example.org foobar@example.org
#
## send mail when get linkDown
#traphandle .1.3.6.1.6.3.1.1.5.3 /usr/bin/traptoemail -s smtp.example.org foobar@example.org
[snmp] logOption f /var/log/snmp/snmptrapd.log
#createUser -e 0x0102030405 user1 SHA authphrase1 AES privacyphrase1
#authUser log,execute,net user1 noauth
#createUser -e 0x0102030405 user2 SHA authphrase2 AES privacyphrase2
#authUser log,execute,net user2 noauth
#createUser -e 0x0102030405 user3 SHA authphrase3 AES privacyphrase3
#authUser log,execute,net user3 noauth
#createUser -e 0x0102030405 user4 SHA authphrase4 AES privacyphrase4
#authUser log,execute,net user4 noauth
#createUser -e 0x0102030405 user5 SHA authphrase5 AES privacyphrase5
#authUser log,execute,net user5 noauth
